## Developer Input 

Never write anything here

## Detailed description 

In lib/ClientState/Session.vim, the RemoveSessionServer() function calls
`SessionServers->filter(...)` at line 14 but discards the return value.

While in Vim9 script, `->filter()` does modify the list in place,
discarding the return value is inconsistent with patterns used elsewhere
in the codebase. For example, GetSessionServerIdsByFt uses
`->copy()->filter()->mapnew()` with proper chaining.

More critically, if SessionServers is a `null_list` alias shared across
module loads, the filter behavior may be unexpected. The in-place
modification may not persist correctly depending on how the list
reference is handled by the Vim9 runtime.

This pattern should be explicit: either assign the result back or use a
clear in-place removal method.

Location: lib/ClientState/Session.vim:14
Severity: medium
